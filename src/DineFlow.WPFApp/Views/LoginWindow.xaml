<Window x:Class="DineFlow.WPFApp.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DineFlow Login" Height="320" Width="420"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="24">
        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="DineFlow Staff Login" FontSize="24" FontWeight="Bold" Margin="0 0 0 24" />
            <TextBlock Text="Username" />
            <TextBox x:Name="txtUsername" Height="32" Margin="0 4 0 12" />
            <TextBlock Text="Password" />
            <PasswordBox x:Name="txtPassword" Height="32" Margin="0 4 0 20" />
            <Button Content="Login" Height="36" Click="btnLogin_Click" />
        </StackPanel>
    </Grid>
</Window>

<Window x:Class="DineFlow.WPFApp.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:DineFlow.WPFApp.ViewModels"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        d:DataContext="{d:DesignInstance Type=viewmodels:LoginViewModel}"
        mc:Ignorable="d"
        Title="Login - DineFlow" Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{DynamicResource WindowBackgroundBrush}"
        FocusManager.FocusedElement="{Binding ElementName=txtUsername}">

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard Storyboard="{StaticResource WindowFadeInStoryboard}" />
        </EventTrigger>
    </Window.Triggers>

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="420" />
        </Grid.ColumnDefinitions>

        <!-- Left Side: Brand Area -->
        <Border Grid.Column="0" Background="{DynamicResource BrandPrimaryBrush}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="40">
                <iconPacks:PackIconMaterial Kind="SilverwareForkKnife" Width="96" Height="96" Foreground="{DynamicResource TextOnAccentBrush}" HorizontalAlignment="Center" Margin="0,0,0,24" />
                <TextBlock Text="DineFlow" FontFamily="{StaticResource PrimaryFontFamily}" FontSize="48" FontWeight="Bold" Foreground="{DynamicResource TextOnAccentBrush}" HorizontalAlignment="Center" Margin="0,0,0,16" />
                <TextBlock Text="Modern Restaurant Management" FontFamily="{StaticResource PrimaryFontFamily}" FontSize="18" FontWeight="Medium" Foreground="{DynamicResource TextOnAccentBrush}" Opacity="0.85" HorizontalAlignment="Center" />
            </StackPanel>
        </Border>

        <!-- Right Side: Login Form -->
        <Border Grid.Column="1" Background="{DynamicResource CardBackgroundBrush}">
            <Grid Margin="48">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Welcome Back" Style="{StaticResource ApplicationTitleStyle}" Margin="0,0,0,8" />
                    <TextBlock Text="Please sign in to your account." Style="{StaticResource BodyTextStyle}" FontSize="15" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,48" />

                    <TextBlock Text="Username" Style="{StaticResource BodyTextStyle}" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <TextBox x:Name="txtUsername" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" 
                             Style="{StaticResource StandardTextBoxStyle}" Margin="0,0,0,24" />

                    <TextBlock Text="Password" Style="{StaticResource BodyTextStyle}" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <!-- Securely forward password value via Code-Behind to preserve MVVM boundaries -->
                    <PasswordBox x:Name="txtPassword" PasswordChanged="txtPassword_PasswordChanged" 
                                 Style="{StaticResource StandardPasswordBoxStyle}" Margin="0,0,0,8" />
                                 
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="{DynamicResource ActionDangerBrush}" 
                               Style="{StaticResource BodyTextStyle}" FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,16,0,8" />

                    <Button Content="Sign In" Command="{Binding LoginCommand}" 
                            IsDefault="True" Style="{StaticResource PrimaryButtonStyle}" Height="44" FontSize="16" Margin="0,24,0,0" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Loading Overlay -->
        <Border Grid.ColumnSpan="2" Background="{DynamicResource OverlayBackgroundBrush}" 
                Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"
                Panel.ZIndex="10">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4" Foreground="{DynamicResource BrandPrimaryBrush}" BorderThickness="0" Background="Transparent" />
                <TextBlock Text="Authenticating..." Style="{StaticResource BodyTextStyle}" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,15,0,0" Foreground="{DynamicResource BrandPrimaryBrush}" />
            </StackPanel>
        </Border>
    </Grid>
</Window>

<Window x:Class="DineFlow.WPFApp.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:DineFlow.WPFApp.ViewModels"
        d:DataContext="{d:DesignInstance Type=viewmodels:LoginViewModel}"
        mc:Ignorable="d"
        Title="Login - DineFlow" Height="400" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="DineFlow" FontSize="32" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,30"/>

        <TextBlock Grid.Row="1" Text="Username:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="2" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" 
                 Margin="0,0,0,15" Padding="5" Height="30"/>

        <TextBlock Grid.Row="3" Text="Password:" Margin="0,0,0,5"/>
        <!-- Securely forward password value via Code-Behind to preserve MVVM boundaries -->
        <PasswordBox Grid.Row="4" x:Name="txtPassword" PasswordChanged="txtPassword_PasswordChanged" 
                     Margin="0,0,0,15" Padding="5" Height="30"/>

        <!-- Error Message -->
        <TextBlock Grid.Row="5" Text="{Binding ErrorMessage}" Foreground="Red" 
                   TextWrapping="Wrap" Margin="0,0,0,15" 
                   HorizontalAlignment="Center" FontWeight="SemiBold"/>

        <!-- Action Row: Progress Bar & Login Button -->
        <Grid Grid.Row="6" VerticalAlignment="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- Loading Spinner (Indeterminate Progress Bar) -->
            <ProgressBar Grid.Column="0" IsIndeterminate="True" 
                         Margin="0,0,15,0" Height="20"
                         Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" />

            <Button Grid.Column="1" Content="Login" Command="{Binding LoginCommand}" 
                    IsDefault="True"
                    Width="100" Height="35" Padding="10,5"/>
        </Grid>
    </Grid>
</Window>

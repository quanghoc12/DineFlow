<Window x:Class="DineFlow.WPFApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:DineFlow.WPFApp.ViewModels"
        d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
        mc:Ignorable="d"
        Title="DineFlow - Main Shell" Height="768" Width="1024"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- HEADER -->
        <Border Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Background="#2C3E50">
            <Grid Margin="15,0">
                <TextBlock Text="DineFlow App" Foreground="White" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
                
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <TextBlock Text="{Binding CurrentUserName}" Foreground="White" FontSize="16" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding CurrentRole, StringFormat='({0})'}" Foreground="#BDC3C7" FontSize="14" Margin="0,0,20,0" VerticalAlignment="Center"/>
                    <Button Content="Logout" Command="{Binding LogoutCommand}" Padding="15,5" Background="#E74C3C" Foreground="White" BorderThickness="0" Cursor="Hand"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- SIDEBAR -->
        <Border Grid.Row="1" Grid.Column="0" Background="#34495E">
            <ItemsControl ItemsSource="{Binding MenuItems}" Margin="0,15,0,0">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <!-- Bind Command to the MainViewModel while CommandParameter is the MenuItemViewModel itself -->
                        <Button Content="{Binding Title}" 
                                Command="{Binding DataContext.NavigateCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                CommandParameter="{Binding}"
                                Margin="10,5" Padding="10" 
                                HorizontalContentAlignment="Left"
                                Background="Transparent" Foreground="White" BorderThickness="0" Cursor="Hand" FontSize="16"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>

        <!-- CONTENT AREA -->
        <Border Grid.Row="1" Grid.Column="1" Background="#ECF0F1" Padding="20">
            <!-- 4. ContentControl bound to CurrentViewModel. Automatic resolution happens via DataTemplates. -->
            <ContentControl Content="{Binding CurrentViewModel}" />
        </Border>
    </Grid>
</Window>

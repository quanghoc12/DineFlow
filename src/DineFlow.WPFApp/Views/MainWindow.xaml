<Window x:Class="DineFlow.WPFApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:DineFlow.WPFApp.ViewModels"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
        mc:Ignorable="d"
        Title="DineFlow - Main Shell" Height="768" Width="1024"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        Background="{DynamicResource WindowBackgroundBrush}">

    <WindowChrome.WindowChrome>
        <WindowChrome GlassFrameThickness="0" CornerRadius="0" CaptionHeight="60" UseAeroCaptionButtons="False" />
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <!-- Window Control Buttons Styles -->
        <Style TargetType="Button" x:Key="WindowControlButtonStyle">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E2E8F0"/>
                    <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="Button" x:Key="WindowCloseButtonStyle" BasedOn="{StaticResource WindowControlButtonStyle}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#EF4444"/>
                    <Setter Property="Foreground" Value="#FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard Storyboard="{StaticResource WindowFadeInStoryboard}" />
        </EventTrigger>
    </Window.Triggers>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- SIDEBAR -->
        <Border Grid.Row="0" Grid.RowSpan="2" Grid.Column="0" Background="{DynamicResource SidebarBackgroundBrush}" BorderThickness="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="60" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                
                <!-- Logo Area -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="24,0,0,0">
                    <iconPacks:PackIconMaterial Kind="SilverwareForkKnife" Width="28" Height="28" Foreground="{DynamicResource BrandPrimaryBrush}" Margin="0,0,12,0" VerticalAlignment="Center"/>
                    <TextBlock Text="DineFlow" FontFamily="{StaticResource PrimaryFontFamily}" FontSize="24" FontWeight="Bold" Foreground="#FFFFFF" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Navigation -->
                <ItemsControl Grid.Row="1" ItemsSource="{Binding MenuItems}" Margin="12,24,12,0" WindowChrome.IsHitTestVisibleInChrome="True">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Command="{Binding DataContext.NavigateCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                    CommandParameter="{Binding}"
                                    Style="{StaticResource SidebarButtonStyle}"
                                    Margin="0,0,0,8">
                                <StackPanel Orientation="Horizontal">
                                    <iconPacks:PackIconMaterial Kind="ChevronRight" Width="16" Height="16" Margin="0,0,16,0" VerticalAlignment="Center" Foreground="#94A3B8"/>
                                    <TextBlock Text="{Binding Title}" VerticalAlignment="Center" FontSize="15" />
                                </StackPanel>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </Border>

        <!-- HEADER -->
        <Border Grid.Row="0" Grid.Column="1" Background="{DynamicResource HeaderBackgroundBrush}" BorderBrush="{DynamicResource DividerBrush}" BorderThickness="0,0,0,1">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,140,0">
                    <iconPacks:PackIconMaterial Kind="AccountCircle" Width="24" Height="24" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding CurrentUserName}" Style="{StaticResource BodyTextStyle}" FontWeight="SemiBold" Margin="0,0,8,0" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding CurrentRole, StringFormat='({0})'}" Style="{StaticResource CaptionTextStyle}" Margin="0,0,32,0" VerticalAlignment="Center"/>
                    <Button WindowChrome.IsHitTestVisibleInChrome="True" Content="Logout" Command="{Binding LogoutCommand}" Style="{StaticResource StandardButtonStyle}" Padding="16,8" />
                </StackPanel>
                
                <!-- System Window Controls -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" WindowChrome.IsHitTestVisibleInChrome="True">
                    <Button Click="BtnMinimize_Click" Style="{StaticResource WindowControlButtonStyle}" Width="46" Height="32">
                        <iconPacks:PackIconMaterial Kind="WindowMinimize" Width="12" Height="12" />
                    </Button>
                    <Button Click="BtnMaximize_Click" Style="{StaticResource WindowControlButtonStyle}" Width="46" Height="32">
                        <iconPacks:PackIconMaterial Kind="WindowMaximize" Width="12" Height="12" />
                    </Button>
                    <Button Click="BtnClose_Click" Style="{StaticResource WindowCloseButtonStyle}" Width="46" Height="32">
                        <iconPacks:PackIconMaterial Kind="Close" Width="12" Height="12" />
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- CONTENT AREA -->
        <!-- Adding some padding inside the window background so it looks like floating content -->
        <Border Grid.Row="1" Grid.Column="1" Background="{DynamicResource WindowBackgroundBrush}">
            <ContentControl Content="{Binding CurrentViewModel}" Margin="32" />
        </Border>
    </Grid>
</Window>

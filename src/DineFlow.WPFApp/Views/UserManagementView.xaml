<UserControl x:Class="DineFlow.WPFApp.Views.UserManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:DineFlow.WPFApp.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:UserManagementViewModel}"
             d:DesignHeight="600" d:DesignWidth="800">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="User Management" FontSize="24" FontWeight="Bold" Margin="20,20,20,10" Foreground="#2C3E50"/>

        <!-- Toolbar (Search and Actions) -->
        <Grid Grid.Row="1" Margin="20,0,20,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                     Height="30" Padding="5" VerticalAlignment="Center" />
            <TextBlock Grid.Column="0" Text="Search Users..." IsHitTestVisible="False" Foreground="Gray" 
                       Margin="10,0,0,0" VerticalAlignment="Center">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SearchText}" Value="">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <!-- Actions -->
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button Content="Create New User" Command="{Binding CreateUserCommand}" Margin="0,0,10,0" Padding="15,5" Background="#2ECC71" Foreground="White" BorderThickness="0"/>
                <Button Content="Edit" Command="{Binding UpdateUserCommand}" Margin="0,0,10,0" Padding="15,5" Background="#F39C12" Foreground="White" BorderThickness="0"/>
                <Button Content="Reset Password" Command="{Binding ResetPasswordCommand}" Margin="0,0,10,0" Padding="15,5" Background="#3498DB" Foreground="White" BorderThickness="0"/>
                <Button Content="Disable" Command="{Binding DisableUserCommand}" Margin="0,0,10,0" Padding="15,5" Background="#E74C3C" Foreground="White" BorderThickness="0"/>
                <Button Content="Enable" Command="{Binding EnableUserCommand}" Padding="15,5" Background="#27AE60" Foreground="White" BorderThickness="0"/>
            </StackPanel>
        </Grid>

        <!-- Error Message -->
        <TextBlock Grid.Row="1" Text="{Binding ErrorMessage}" Foreground="Red" 
                   HorizontalAlignment="Center" Margin="0,40,0,0" FontWeight="SemiBold"/>

        <!-- Data Grid -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedUser}" 
                  AutoGenerateColumns="False" IsReadOnly="True" Margin="20"
                  HeadersVisibility="Column" SelectionMode="Single" SelectionUnit="FullRow"
                  GridLinesVisibility="Horizontal" RowHeight="35">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="*"/>
                <DataGridTextColumn Header="Full Name" Binding="{Binding FullName}" Width="1.5*"/>
                <DataGridTextColumn Header="Role" Binding="{Binding RoleName}" Width="*"/>
                <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="80"/>
                <DataGridTextColumn Header="Created At" Binding="{Binding CreatedAt, StringFormat=MM/dd/yyyy}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Loading Indicator -->
        <ProgressBar Grid.Row="3" IsIndeterminate="True" Height="4" BorderThickness="0" 
                     Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}" />
    </Grid>
</UserControl>

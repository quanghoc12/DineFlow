<UserControl x:Class="DineFlow.WPFApp.Views.UserManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Margin="0,0,0,18">
            <StackPanel>
                <TextBlock Text="Quản lý tài khoản"
                           FontSize="28"
                           FontWeight="Bold"
                           Foreground="#102A43" />
                <TextBlock Text="Thêm, chỉnh sửa và kiểm soát quyền truy cập của nhân viên."
                           Margin="0,6,0,0"
                           Foreground="#627D98" />
            </StackPanel>
            <Button Content="+ Thêm người dùng"
                    Click="CreateButton_Click"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Padding="16,10"
                    Background="#2F80ED"
                    Foreground="White"
                    BorderThickness="0"
                    FontWeight="SemiBold" />
        </Grid>

        <Border Grid.Row="1"
                Background="White"
                CornerRadius="10,10,0,0"
                Padding="16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="300" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Padding="10,7"
                         ToolTip="Tìm theo tên đăng nhập hoặc họ tên" />
                <TextBlock Grid.Column="1"
                           Text="{Binding ErrorMessage}"
                           Foreground="#C53030"
                           VerticalAlignment="Center"
                           Margin="16,0" />
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Content="Sửa" Click="EditButton_Click" Padding="13,7" Margin="0,0,8,0" />
                    <Button Content="Đặt lại mật khẩu" Click="ResetPasswordButton_Click" Padding="13,7" Margin="0,0,8,0" />
                    <Button Content="Khóa / Mở khóa" Click="ToggleActiveButton_Click" Padding="13,7" />
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2" Background="White" CornerRadius="0,0,10,10">
            <DataGrid ItemsSource="{Binding Users}"
                      SelectedItem="{Binding SelectedUser}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      SelectionUnit="FullRow"
                      BorderThickness="0"
                      GridLinesVisibility="Horizontal"
                      CanUserAddRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Tên đăng nhập" Binding="{Binding Username}" Width="*" />
                    <DataGridTextColumn Header="Họ và tên" Binding="{Binding FullName}" Width="1.5*" />
                    <DataGridTextColumn Header="Vai trò" Binding="{Binding Role}" Width="110" />
                    <DataGridCheckBoxColumn Header="Hoạt động" Binding="{Binding IsActive}" Width="100" />
                    <DataGridTextColumn Header="Ngày tạo"
                                        Binding="{Binding CreatedAt, StringFormat=dd/MM/yyyy}"
                                        Width="130" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>

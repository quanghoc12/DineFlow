<Window x:Class="DineFlow.WPFApp.Features.Management.Menu.ChoiceItemChannelPriceEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Giá lựa chọn theo kênh bán" Width="560" Height="360"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent" ResizeMode="NoResize">
    <Border Background="White" BorderBrush="#0866E5" BorderThickness="2" CornerRadius="16" MouseLeftButtonDown="Window_MouseLeftButtonDown">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel>
                <TextBlock x:Name="TitleText" Text="Cấu hình giá lựa chọn phụ theo kênh" FontSize="18" FontWeight="Bold" />
                <TextBlock x:Name="SubtitleText" Text="Điều chỉnh giá bán phụ thu cho lựa chọn phụ này trên kênh bán."
                           Foreground="#64748B"
                           TextWrapping="Wrap"
                           Margin="0,4,0,0" />
            </StackPanel>

            <StackPanel Grid.Row="1" Margin="0,16,0,16">
                <TextBlock Text="Giá gốc bán tại quán" Margin="0,0,0,4" Foreground="#64748B" />
                <TextBlock x:Name="BasePriceText" Text="0 ₫" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,12" />

                <TextBlock Text="Giá phụ thu thêm (cộng thêm)" Margin="0,0,0,6" FontWeight="SemiBold" />
                <TextBox x:Name="ExtraPriceTextBox" Text="0" Padding="9,6" />
            </StackPanel>

            <StackPanel Grid.Row="2">
                <TextBlock x:Name="ErrorText" Foreground="#C53030" TextWrapping="Wrap" Margin="0,0,0,10" Height="Auto" />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Hủy" Click="CancelButton_Click" Padding="15,7" Margin="0,0,8,0" />
                    <Button Content="Lưu giá" Click="SaveButton_Click" Padding="15,7" />
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
</Window>

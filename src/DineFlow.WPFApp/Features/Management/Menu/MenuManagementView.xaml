<UserControl x:Class="DineFlow.WPFApp.Features.Management.Menu.MenuManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Margin="0,0,0,18">
            <StackPanel>
                <TextBlock Text="Quản lý thực đơn" FontSize="28" FontWeight="Bold" Foreground="#102A43" />
                <TextBlock Text="Danh mục, giá bán, tồn kho và trạng thái món." Margin="0,6,0,0" Foreground="#627D98" />
            </StackPanel>
            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="Danh mục" Click="CategoryButton_Click" Padding="16,10" Margin="0,0,8,0" />
                <Button Content="Lựa chọn &amp; giá kênh" Click="ChoicePricingButton_Click" Padding="16,10" Margin="0,0,8,0" />
                <Button Content="+ Thêm món" Click="CreateButton_Click" Padding="16,10"
                        Background="#2F80ED" Foreground="White" BorderThickness="0" />
            </StackPanel>
        </Grid>
        <Border Grid.Row="1" Background="White" CornerRadius="10,10,0,0" Padding="16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="270" />
                    <ColumnDefinition Width="220" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Padding="10,7" ToolTip="Tìm món" />
                <ComboBox Grid.Column="1" ItemsSource="{Binding Categories}" DisplayMemberPath="CategoryName"
                          SelectedItem="{Binding CategoryFilter}" Margin="10,0,0,0" Padding="8,6" />
                <TextBlock Grid.Column="2" Text="{Binding ErrorMessage}" Foreground="#C53030"
                           VerticalAlignment="Center" Margin="14,0" />
                <StackPanel Grid.Column="3" Orientation="Horizontal">
                    <Button Content="Sửa" Click="EditButton_Click" Padding="14,7" Margin="0,0,8,0" />
                    <Button Content="Mở / Tạm ngưng bán" Click="ToggleButton_Click" Padding="14,7" />
                </StackPanel>
            </Grid>
        </Border>
        <Border Grid.Row="2" Background="White" CornerRadius="0,0,10,10">
            <DataGrid ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem}"
                      AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
                      SelectionMode="Single" SelectionUnit="FullRow" BorderThickness="0"
                      GridLinesVisibility="Horizontal">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Tên món" Binding="{Binding Name}" Width="1.5*" />
                    <DataGridTextColumn Header="Danh mục" Binding="{Binding CategoryName}" Width="*" />
                    <DataGridTextColumn Header="Giá" Binding="{Binding BasePrice, StringFormat={}{0:N0} ₫}" Width="130" />
                    <DataGridTextColumn Header="Tồn kho" Binding="{Binding Stock}" Width="100" />
                    <DataGridCheckBoxColumn Header="Đang bán" Binding="{Binding IsAvailable}" Width="100" />
                    <DataGridTextColumn Header="Ảnh" Binding="{Binding ImageUrl}" Width="1.5*" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>

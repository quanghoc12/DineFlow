<Window x:Class="DineFlow.WPFApp.Features.Reports.Dashboard.DashboardAssistantWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Chatbot AI"
        Width="520"
        Height="640"
        MinWidth="460"
        MinHeight="540"
        WindowStartupLocation="CenterOwner"
        Background="#F4F7FB">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <Style x:Key="SendButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2563EB" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#2563EB" />
            <Setter Property="Padding" Value="16,8" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
    </Window.Resources>

    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Background="#0B55A0" CornerRadius="8" Padding="14" Margin="0,0,0,10">
            <StackPanel>
                <TextBlock Text="Chatbot AI phân tích dashboard"
                           Foreground="White"
                           FontSize="18"
                           FontWeight="Bold" />
                <TextBlock Text="Phân tích dữ liệu theo câu hỏi, chỉ đọc dữ liệu báo cáo"
                           Foreground="#DBEAFE"
                           FontSize="12"
                           Margin="0,4,0,0" />
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Background="White" CornerRadius="8" BorderBrush="#E2E8F0" BorderThickness="1">
            <ScrollViewer x:Name="MessagesScrollViewer" VerticalScrollBarVisibility="Auto" Padding="12">
                <ItemsControl ItemsSource="{Binding Messages}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#F8FAFC"
                                    BorderBrush="#E2E8F0"
                                    BorderThickness="1"
                                    CornerRadius="8"
                                    Padding="10"
                                    Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock FontWeight="Bold" Foreground="#0F172A">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Text" Value="AI" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Role}" Value="user">
                                                        <Setter Property="Text" Value="Bạn" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    <TextBlock Text="{Binding Content}"
                                               TextWrapping="Wrap"
                                               Foreground="#1E293B"
                                               Margin="0,6,0,0" />
                                    <TextBlock Text="{Binding CreatedAt, StringFormat={}{0:HH:mm}}"
                                               Foreground="#94A3B8"
                                               FontSize="11"
                                               HorizontalAlignment="Right"
                                               Margin="0,6,0,0" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <ItemsControl Grid.Row="2" ItemsSource="{Binding SuggestedQuestions}" Margin="0,10,0,6">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Content="{Binding}"
                            Click="SuggestedQuestionButton_Click"
                            Tag="{Binding}"
                            Margin="0,0,6,6"
                            Padding="8,5"
                            Background="#E0F2FE"
                            Foreground="#075985"
                            BorderBrush="#BAE6FD"
                            Cursor="Hand" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Grid Grid.Row="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Text="{Binding ErrorMessage}"
                       Foreground="#DC2626"
                       FontWeight="SemiBold"
                       TextWrapping="Wrap"
                       Margin="0,0,0,6" />

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBox Text="{Binding CurrentMessage, UpdateSourceTrigger=PropertyChanged}"
                         MinHeight="38"
                         Padding="10,8"
                         VerticalContentAlignment="Center"
                         BorderBrush="#CBD5E1"
                         KeyDown="MessageTextBox_KeyDown" />
                <Button Grid.Column="1"
                        Content="Gửi"
                        Command="{Binding SendCommand}"
                        Style="{StaticResource SendButtonStyle}"
                        Margin="8,0,0,0" />
            </Grid>
        </Grid>

        <Border Grid.RowSpan="4"
                Background="#80FFFFFF"
                Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}">
            <TextBlock Text="Đang hỏi DeepSeek..."
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontWeight="Bold"
                       Foreground="#0F172A" />
        </Border>
    </Grid>
</Window>
